# controls code coverage through coverage.py

[run]
branch = True

[report]
exclude_lines =
    # Don't complain if non-runnable code isn't run:
    if __name__ == .__main__.:
    if submit:

ignore_errors = True
